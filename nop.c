#include "monty.h"

/**
 * nop_operation - Does nothing, a placeholder operation
 * @stack: Pointer to the stack
 * @line_number: Line number in the monty file
 * @Return: No return
 */
void nop_operation(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
